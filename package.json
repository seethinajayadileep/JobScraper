{
  "name": "job-discovery",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered Job Discovery — scrape, rank, and discover the best opportunities",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "concurrently -n backend,frontend -c cyan,magenta \"npm run start -w backend\" \"npm run start -w frontend\"",
    "lint": "npm run lint -w backend && npm run lint -w frontend",
    "test": "npm run test -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
